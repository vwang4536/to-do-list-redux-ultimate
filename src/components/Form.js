import React from 'react';

const Form = () => {

  return (
    <div>
      <form>
        <input type="text" />
        <button type="submit">Submit</button>
      </form>
    </div>
  )
};

export default Form;
